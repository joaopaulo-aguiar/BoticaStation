// GraphQL queries will be placed here as modules are implemented
// Example:
// export const GET_CONTACTS = `
//   query GetContacts($limit: Int, $nextToken: String) {
//     listContacts(limit: $limit, nextToken: $nextToken) {
//       items { id name email phone lifecycleStage status }
//       nextToken
//     }
//   }
// `
